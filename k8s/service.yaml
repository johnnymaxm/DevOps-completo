apiVersion: v1
kind: Service
metadata:
  name: devops-service
  namespace: default
  labels:
    app: devops-app
spec:
  selector:
    app: devops-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP